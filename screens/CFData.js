import { View, Text } from "react-native";
import React from "react";
import KatibuData from "./KatibuData";

const CFData = ({ route }) => {
  return <KatibuData route={route} />;
};

export default CFData;
